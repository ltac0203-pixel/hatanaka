<?php

return [

    /*
    |--------------------------------------------------------------------------
    | 通知やUIで参照するアプリ名を決める
    |--------------------------------------------------------------------------
    |
    | この値はアプリケーション名です。
    | 通知文や各種UIで名称表示が必要になったときに利用されます。
    |
    */

    'name' => env('APP_NAME', 'Laravel'),

    /*
    |--------------------------------------------------------------------------
    | 実行環境ごとの挙動切替基準を決める
    |--------------------------------------------------------------------------
    |
    | この値は現在の実行環境を表します。
    | 環境ごとのサービス設定を切り替える前提になるため `.env` で設定します。
    |
    */

    'env' => env('APP_ENV', 'production'),

    /*
    |--------------------------------------------------------------------------
    | 例外詳細をどこまで公開するかを決める
    |--------------------------------------------------------------------------
    |
    | 有効にすると詳細な例外情報とスタックトレースを表示します。
    | 無効時は利用者向けの簡易エラーページだけを返します。
    |
    */

    'debug' => (bool) env('APP_DEBUG', false),

    /*
    |--------------------------------------------------------------------------
    | コンソール生成URLの基準を揃える
    |--------------------------------------------------------------------------
    |
    | このURLは Artisan 実行時のURL生成に使われます。
    | コンソール経由でも正しい絶対URLを組み立てられるようルートURLを設定します。
    |
    */

    'url' => env('APP_URL', 'http://localhost'),

    /*
    |--------------------------------------------------------------------------
    | 日付計算の基準時刻を揃える
    |--------------------------------------------------------------------------
    |
    | ここではアプリケーションの既定タイムゾーンを指定します。
    | PHP の日付処理全体で使われる基準時刻になります。
    |
    */

    'timezone' => 'Asia/Tokyo',

    /*
    |--------------------------------------------------------------------------
    | 既定の表示言語を揃える
    |--------------------------------------------------------------------------
    |
    | アプリケーション既定のロケールを決めます。
    | Laravel の翻訳・ローカライズ処理はこの値を初期値として利用します。
    |
    */

    'locale' => env('APP_LOCALE', 'ja'),

    'fallback_locale' => env('APP_FALLBACK_LOCALE', 'en'),

    'faker_locale' => env('APP_FAKER_LOCALE', 'ja_JP'),

    /*
    |--------------------------------------------------------------------------
    | 暗号化の前提となる秘密鍵を定める
    |--------------------------------------------------------------------------
    |
    | このキーは Laravel の暗号化処理で利用されます。
    | 暗号化データを安全に保つため、デプロイ前に十分ランダムな値を設定してください。
    |
    */

    'cipher' => 'AES-256-CBC',

    'key' => env('APP_KEY'),

    'previous_keys' => [
        ...array_filter(
            explode(',', (string) env('APP_PREVIOUS_KEYS', ''))
        ),
    ],

    /*
    |--------------------------------------------------------------------------
    | メンテナンス状態の共有方法を決める
    |--------------------------------------------------------------------------
    |
    | ここではメンテナンスモードの状態管理方法を決めます。
    | `cache` を使うと複数台構成でも同じ状態を共有できます。
    |
    | 利用可能: "file", "cache"
    |
    */

    'maintenance' => [
        'driver' => env('APP_MAINTENANCE_DRIVER', 'file'),
        'store' => env('APP_MAINTENANCE_STORE', 'database'),
    ],

];
